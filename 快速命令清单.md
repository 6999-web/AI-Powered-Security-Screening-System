# 快速命令清单

## 🚀 一键启动

### Windows 系统

#### 方法 1：分别启动（推荐）

**终端 1 - 启动后端：**
```cmd
cd backend
python main.py
```

**终端 2 - 启动前端：**
```cmd
npm run dev
```

#### 方法 2：创建启动脚本

创建 `start.bat` 文件：
```batch
@echo off
echo 正在启动智能安检系统...
echo.

echo [1/2] 启动后端服务...
start cmd /k "cd backend && python main.py"
timeout /t 3

echo [2/2] 启动前端服务...
start cmd /k "npm run dev"

echo.
echo ✅ 系统启动完成！
echo.
echo 后端地址: http://localhost:8888
echo 前端地址: http://localhost:5173
echo API 文档: http://localhost:8888/docs
pause
```

双击 `start.bat` 即可启动。

### Linux/Mac 系统

创建 `start.sh` 文件：
```bash
#!/bin/bash

echo "正在启动智能安检系统..."
echo ""

echo "[1/2] 启动后端服务..."
cd backend
python main.py &
BACKEND_PID=$!
cd ..

sleep 3

echo "[2/2] 启动前端服务..."
npm run dev &
FRONTEND_PID=$!

echo ""
echo "✅ 系统启动完成！"
echo ""
echo "后端地址: http://localhost:8888"
echo "前端地址: http://localhost:5173"
echo "API 文档: http://localhost:8888/docs"
echo ""
echo "按 Ctrl+C 停止服务"

wait
```

运行：
```bash
chmod +x start.sh
./start.sh
```

## 📦 安装依赖

### 首次安装（只需运行一次）

```bash
# 1. 安装后端依赖
cd backend
pip install -r requirements.txt
cd ..

# 2. 安装前端依赖
npm install
```

### 更新依赖

```bash
# 更新后端依赖
cd backend
pip install -r requirements.txt --upgrade
cd ..

# 更新前端依赖
npm update
```

## 🧪 测试命令

### 测试 API 连接

创建 `backend/test_api.py`：
```python
from openai import OpenAI
import os
from dotenv import load_dotenv

load_dotenv()

client = OpenAI(
    api_key=os.getenv("DASHSCOPE_API_KEY"),
    base_url="https://dashscope.aliyuncs.com/compatible-mode/v1"
)

print("正在测试 API 连接...")
response = client.chat.completions.create(
    model="qwen3-vl-plus",
    messages=[{"role": "user", "content": "你好"}]
)

print("✅ API 连接成功！")
print(f"响应：{response.choices[0].message.content}")
```

运行测试：
```bash
cd backend
python test_api.py
```

### 测试后端接口

```bash
# 测试根路径
curl http://localhost:8888/

# 测试 API 文档
# 浏览器访问：http://localhost:8888/docs
```

## 🔧 常用命令

### 查看端口占用

**Windows:**
```cmd
# 查看 8888 端口
netstat -ano | findstr :8888

# 查看 5173 端口
netstat -ano | findstr :5173

# 结束进程（替换 PID）
taskkill /PID <进程ID> /F
```

**Linux/Mac:**
```bash
# 查看 8888 端口
lsof -i :8888

# 查看 5173 端口
lsof -i :5173

# 结束进程
kill -9 <PID>
```

### 清理缓存

```bash
# 清理前端缓存
rm -rf node_modules
npm install

# 清理 Python 缓存
cd backend
find . -type d -name "__pycache__" -exec rm -r {} +
find . -type f -name "*.pyc" -delete
```

### 查看日志

```bash
# 后端日志（终端输出）
# 直接查看运行 python main.py 的终端

# 前端日志（浏览器控制台）
# 按 F12 打开开发者工具 → Console 标签
```

## 📝 配置文件

### 后端配置（backend/.env）

```bash
# 编辑配置
notepad backend\.env  # Windows
nano backend/.env     # Linux/Mac

# 配置内容
DASHSCOPE_API_KEY=sk-88ed49180e7d4462b16b8f2902f63c7a
QWEN_MODEL=qwen3-vl-plus
QWEN_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
MAX_IMAGE_SIZE_MB=10
ENABLE_CACHE=true
```

### 前端配置（vite.config.js）

```javascript
export default defineConfig({
  server: {
    port: 5173,
    proxy: {
      '/api': {
        target: 'http://localhost:8888',
        changeOrigin: true
      }
    }
  }
})
```

## 🛑 停止服务

### 方法 1：终端停止

在运行服务的终端按 `Ctrl + C`

### 方法 2：结束进程

**Windows:**
```cmd
# 结束后端
taskkill /F /IM python.exe

# 结束前端
taskkill /F /IM node.exe
```

**Linux/Mac:**
```bash
# 结束后端
pkill -f "python main.py"

# 结束前端
pkill -f "vite"
```

## 🔄 重启服务

```bash
# 1. 停止服务（Ctrl + C）
# 2. 重新启动

# 后端
cd backend
python main.py

# 前端
npm run dev
```

## 📊 性能监控

### 查看系统资源

**Windows:**
```cmd
# 打开任务管理器
taskmgr

# 查看 Python 和 Node 进程的 CPU、内存占用
```

**Linux/Mac:**
```bash
# 查看进程资源
top

# 或使用 htop（需要安装）
htop
```

### 查看 API 调用统计

后端终端会显示每次 API 调用的日志：
```
收到图片，大小：245678 字节
正在调用 AI 识别...
识别成功，共 3 个物品
```

## 🎯 快速访问链接

| 服务 | 地址 | 说明 |
|------|------|------|
| 前端界面 | http://localhost:5173 | 用户界面 |
| 后端 API | http://localhost:8888 | API 服务 |
| API 文档 | http://localhost:8888/docs | Swagger 文档 |
| 入场安检 | http://localhost:5173/checkin | 直接访问入场页面 |
| 离场安检 | http://localhost:5173/checkout | 直接访问离场页面 |
| 数据看板 | http://localhost:5173/dashboard | 数据统计 |

## 🐛 故障排查

### 后端启动失败

```bash
# 检查 Python 版本
python --version  # 需要 3.10+

# 检查依赖
pip list

# 重新安装依赖
cd backend
pip install -r requirements.txt --force-reinstall
```

### 前端启动失败

```bash
# 检查 Node 版本
node --version  # 需要 16+

# 检查依赖
npm list

# 重新安装依赖
rm -rf node_modules package-lock.json
npm install
```

### 摄像头无法调用

1. 检查浏览器权限
2. 确保使用 localhost（不是 127.0.0.1）
3. 检查摄像头是否被其他程序占用
4. 尝试使用 Chrome 或 Edge 浏览器

### API 调用失败

```bash
# 1. 检查 API Key
cat backend/.env  # Linux/Mac
type backend\.env  # Windows

# 2. 测试 API 连接
cd backend
python test_api.py

# 3. 查看后端日志
# 查看运行 python main.py 的终端输出
```

## 📚 相关文档

- `快速启动指南.md` - 详细的启动步骤
- `摄像头功能测试指南.md` - 功能测试流程
- `开发方案总结.md` - 项目总览
- `CAMERA_INTEGRATION_GUIDE.md` - 技术实现细节

---

**提示：** 建议将常用命令保存为脚本文件，方便快速启动！
