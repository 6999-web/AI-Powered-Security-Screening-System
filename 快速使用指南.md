# 智能安检系统 - 快速使用指南

## 系统已启动

✅ **后端服务**: http://localhost:8888
✅ **前端服务**: http://localhost:5173
✅ **API 文档**: http://localhost:8888/docs

## 访问系统

打开浏览器访问: **http://localhost:5173**

## 功能说明

### 1. 入场安检 (Entry Check)

**步骤**:
1. 点击"入场安检"按钮
2. 输入身份证号（例如：123456789012345678）
3. 上传 X 光图片（拖拽或点击选择）
4. 系统自动识别物品
5. 显示识别结果

**识别结果包含**:
- 物品名称
- 物品类别
- 物品数量
- 物品重量

### 2. 离场安检 (Exit Check)

**步骤**:
1. 点击"离场安检"按钮
2. 输入身份证号（必须与入场时相同）
3. 上传 X 光图片
4. 系统自动与入场记录比对
5. 显示比对结果

**比对结果**:
- ✅ **正常** - 物品一致
- ⚠️ **异常** - 物品不一致（少带、多带或数量异常）

### 3. 数据看板 (Dashboard)

显示系统统计信息：
- 总入场人数
- 总离场人数
- 异常记录数
- 当前在场人数

### 4. 异常记录 (Anomalies)

显示所有异常记录：
- 用户身份证号
- 异常类型
- 异常详情
- 入场时间
- 离场时间

## 测试数据

### 测试场景 1: 正常流程

**入场**:
- 身份证号: `123456789012345678`
- 上传图片: 任意 X 光图片

**离场**:
- 身份证号: `123456789012345678`（相同）
- 上传图片: 相同的 X 光图片

**结果**: 显示"正常"

### 测试场景 2: 异常流程

**入场**:
- 身份证号: `987654321098765432`
- 上传图片: 第一张 X 光图片

**离场**:
- 身份证号: `987654321098765432`（相同）
- 上传图片: 不同的 X 光图片

**结果**: 显示"异常"并记录详情

## API 端点

### 入场安检
```
POST /api/entry
Content-Type: multipart/form-data

参数:
- image: 图片文件
- user_id: 用户 ID
- channel_no: 通道号（可选，默认 1）

响应:
{
  "record_id": "ENTRY_...",
  "user_id": "USER_...",
  "items": [...],
  "total_count": 4,
  "total_weight": 2.1,
  "timestamp": "2026-03-11T..."
}
```

### 离场安检
```
POST /api/exit
Content-Type: multipart/form-data

参数:
- image: 图片文件
- user_id: 用户 ID
- channel_no: 通道号（可选，默认 1）

响应:
{
  "record_id": "EXIT_...",
  "user_id": "USER_...",
  "items": [...],
  "total_count": 4,
  "total_weight": 2.1,
  "comparison_status": "normal" | "anomaly",
  "anomalies": [...],
  "timestamp": "2026-03-11T..."
}
```

### 获取统计数据
```
GET /api/statistics

响应:
{
  "total_entry": 10,
  "total_exit": 8,
  "total_alerts": 2,
  "current_inside": 2
}
```

### 获取最近记录
```
GET /api/recent-records?limit=10

响应:
{
  "records": [...],
  "total": 10
}
```

### 获取异常记录
```
GET /api/anomalies?limit=100

响应:
{
  "anomalies": [...],
  "total": 5
}
```

## 常见问题

### Q: 识别失败怎么办？
A: 
1. 检查网络连接
2. 检查 API Key 是否正确
3. 查看后端日志
4. 尝试上传不同的图片

### Q: 身份证号有什么要求？
A: 
- 任意字符串都可以
- 入场和离场必须使用相同的身份证号
- 系统会自动生成用户 ID

### Q: 如何清空所有数据？
A: 
```bash
curl -X POST http://localhost:8888/api/clear
```

### Q: 如何查看 API 文档？
A: 访问 http://localhost:8888/docs

## 系统架构

```
前端 (Vue 3 + Vite)
    ↓
API 网关 (FastAPI)
    ↓
AI 识别服务 (通义千问 API)
    ↓
数据库 (SQLite)
    ↓
物品比对服务
    ↓
异常检测
```

## 技术栈

- **前端**: Vue 3, Vite, Element Plus
- **后端**: FastAPI, Python 3.8+
- **数据库**: SQLite
- **AI 识别**: 阿里云通义千问 API
- **部署**: 本地开发环境

## 支持

如有问题，请查看：
- 后端日志: `python backend/main_xray_demo.py`
- 前端控制台: F12 打开开发者工具
- API 文档: http://localhost:8888/docs
