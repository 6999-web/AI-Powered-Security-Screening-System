# 🚀 快速启动指南

## 📋 准备工作

### 已完成 ✅
- 外接摄像头已连接
- API Key 已获取：`sk-88ed49180e7d4462b16b8f2902f63c7a`
- 前端界面已完成
- 后端框架已搭建

### 需要安装的软件
- Node.js 16+ 
- Python 3.10+
- 浏览器（Chrome/Edge 推荐）

## 🔧 第一步：配置后端（10 分钟）

### 1. 安装 Python 依赖

```bash
cd backend
pip install -r requirements.txt
```

### 2. 验证 .env 配置

检查 `backend/.env` 文件是否存在，内容应该是：

```bash
DASHSCOPE_API_KEY=sk-88ed49180e7d4462b16b8f2902f63c7a
QWEN_MODEL=qwen3-vl-plus
QWEN_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
MAX_IMAGE_SIZE_MB=10
ENABLE_CACHE=true
```

### 3. 测试 API 连接

创建测试文件 `backend/test_api.py`：

```python
from openai import OpenAI
import os
from dotenv import load_dotenv

load_dotenv()

client = OpenAI(
    api_key=os.getenv("DASHSCOPE_API_KEY"),
    base_url="https://dashscope.aliyuncs.com/compatible-mode/v1"
)

print("正在测试 API 连接...")
response = client.chat.completions.create(
    model="qwen3-vl-plus",
    messages=[{"role": "user", "content": "你好"}]
)

print("✅ API 连接成功！")
print(f"响应：{response.choices[0].message.content}")
```

运行测试：

```bash
python test_api.py
```

如果看到"✅ API 连接成功！"，说明配置正确。

### 4. 启动后端服务

```bash
python main.py
```

看到以下信息说明启动成功：

```
==================================================
系统配置：
  API Key: sk-88ed49...
  模型: qwen3-vl-plus
  缓存: 启用
==================================================
✅ AI 识别服务初始化成功
==================================================
智能安检系统后端服务启动
API 文档: http://localhost:8888/docs
==================================================
```

## 🎨 第二步：配置前端（5 分钟）

### 1. 安装依赖

```bash
cd ..  # 回到项目根目录
npm install
```

### 2. 启动前端服务

```bash
npm run dev
```

看到以下信息说明启动成功：

```
  VITE v5.x.x  ready in xxx ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
```

## 🎥 第三步：测试摄像头功能（5 分钟）

### 1. 打开浏览器

访问：http://localhost:5173

### 2. 进入入场安检页面

点击左侧菜单"入场安检"

### 3. 测试身份验证

- 选择"身份证"方式
- 输入任意身份证号（测试用）
- 点击"验证身份"

### 4. 测试摄像头调用

- 点击"调用摄像头"按钮
- 浏览器会请求摄像头权限，点击"允许"
- 看到摄像头预览画面

### 5. 测试拍照识别

- 将物品放在摄像头前
- 点击"拍照"按钮
- 查看拍摄的图片预览
- 点击"开始识别"按钮
- 等待 2-3 秒
- 查看识别结果

## ✅ 验收标准

### 后端验收
- [ ] 后端服务正常启动（端口 8888）
- [ ] API 文档可访问：http://localhost:8888/docs
- [ ] AI 服务初始化成功
- [ ] 测试 API 连接成功

### 前端验收
- [ ] 前端服务正常启动（端口 5173）
- [ ] 页面可以正常访问
- [ ] 左侧菜单可以切换页面

### 摄像头验收
- [ ] 点击按钮可以调用摄像头
- [ ] 摄像头预览画面正常
- [ ] 可以拍照并预览
- [ ] 可以重新拍摄

### AI 识别验收
- [ ] 点击识别后有加载提示
- [ ] 识别时间 < 5 秒
- [ ] 识别结果正确展示
- [ ] 识别结果包含物品名称、类别、数量

## 🐛 常见问题

### Q1: 后端启动失败

**错误：** `ModuleNotFoundError: No module named 'openai'`

**解决：**
```bash
cd backend
pip install -r requirements.txt
```

### Q2: API 连接失败

**错误：** `invalid_api_key`

**解决：**
1. 检查 `.env` 文件中的 API Key 是否正确
2. 确认 API Key 已激活
3. 检查网络连接

### Q3: 摄像头无法调用

**错误：** `NotAllowedError: Permission denied`

**解决：**
1. 检查浏览器权限设置
2. 确保使用 localhost 或 HTTPS
3. 检查摄像头是否被其他程序占用
4. 尝试刷新页面重新授权

### Q4: 识别结果不准确

**解决：**
1. 提高拍摄图片的清晰度
2. 确保光线充足
3. 物品摆放整齐，避免遮挡
4. 调整摄像头角度

### Q5: 前端无法连接后端

**错误：** `Network Error` 或 `ERR_CONNECTION_REFUSED`

**解决：**
1. 确认后端服务已启动（http://localhost:8888）
2. 检查 `vite.config.js` 中的代理配置
3. 检查防火墙设置

## 📞 获取帮助

如果遇到问题：

1. 查看后端日志（终端输出）
2. 查看浏览器控制台（F12）
3. 查看 API 文档：http://localhost:8888/docs
4. 参考项目文档：
   - `PROJECT_OVERVIEW.md` - 项目概览
   - `QWEN_INTEGRATION_GUIDE.md` - 集成指南
   - `CAMERA_INTEGRATION_GUIDE.md` - 摄像头集成指南
   - `DEVELOPMENT_PLAN.md` - 开发方案

## 🎉 下一步

完成基础功能后，可以：

1. 优化 Prompt 提高识别准确率
2. 添加图片压缩功能降低成本
3. 实现缓存机制节省 API 调用
4. 完善离场安检的摄像头功能
5. 添加异常检测和提示
6. 优化用户界面和交互

---

**预计完成时间：20 分钟**
**难度等级：⭐⭐ 简单**

祝你开发顺利！🚀
