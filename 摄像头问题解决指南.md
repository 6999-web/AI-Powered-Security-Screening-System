# 📷 摄像头问题解决指南

## 🔍 当前问题

错误信息：`NotReadableError: Could not start video source`

这个错误表示：**摄像头被其他程序占用或无法访问**

---

## ✅ 我已经做的修复

我已经更新了代码，添加了：
1. ✅ 更好的错误处理
2. ✅ 降级方案（如果高级配置失败，尝试简单配置）
3. ✅ 更详细的错误提示

---

## 🚀 解决方案

### 方案 1：关闭占用摄像头的程序（最常见）

#### Windows 系统

1. **检查哪些程序在使用摄像头**
   - 按 `Ctrl + Shift + Esc` 打开任务管理器
   - 查找以下程序：
     - 微信
     - QQ
     - 钉钉
     - Zoom
     - Teams
     - Skype
     - 其他视频会议软件

2. **关闭这些程序**
   - 右键点击程序
   - 选择"结束任务"

3. **刷新浏览器**
   ```
   Ctrl + Shift + R
   ```

4. **重新尝试调用摄像头**

### 方案 2：检查浏览器权限

#### Chrome/Edge

1. 点击地址栏左边的锁图标 🔒
2. 查看"摄像头"权限
3. 确保设置为"允许"
4. 刷新页面

#### 或者访问设置

1. 访问：`chrome://settings/content/camera`
2. 确保摄像头未被阻止
3. 确保 `http://localhost:5173` 在允许列表中

### 方案 3：重启浏览器

1. **完全关闭浏览器**（所有窗口）
2. **重新打开浏览器**
3. **访问** `http://localhost:5173`
4. **重新测试**

### 方案 4：使用其他浏览器

如果 Edge 不行，尝试：
- Chrome
- Firefox

### 方案 5：检查摄像头驱动

1. 按 `Win + X`
2. 选择"设备管理器"
3. 展开"照相机"或"图像设备"
4. 右键点击摄像头
5. 选择"更新驱动程序"

### 方案 6：Windows 隐私设置

1. 按 `Win + I` 打开设置
2. 选择"隐私和安全性"
3. 选择"相机"
4. 确保以下选项已开启：
   - "相机访问权限"
   - "允许应用访问相机"
   - "允许桌面应用访问相机"

---

## 🔧 代码已更新

我已经更新了代码，现在会：

### 1. 尝试降级配置

```javascript
// 先尝试高质量配置
try {
  stream = await navigator.mediaDevices.getUserMedia({
    video: {
      width: { ideal: 1280 },
      height: { ideal: 720 },
      facingMode: 'environment'
    }
  })
} catch (err) {
  // 如果失败，使用简单配置
  stream = await navigator.mediaDevices.getUserMedia({
    video: true
  })
}
```

### 2. 提供详细错误信息

- `NotAllowedError` → "摄像头权限被拒绝"
- `NotFoundError` → "未检测到摄像头设备"
- `NotReadableError` → "摄像头被其他程序占用"

---

## 📝 测试步骤

### 步骤 1：刷新浏览器

```
Ctrl + Shift + R
```

### 步骤 2：关闭占用摄像头的程序

检查并关闭：
- 微信
- QQ
- 钉钉
- Zoom
- Teams
- 其他视频软件

### 步骤 3：重新测试

1. 进入"离场安检"页面
2. 输入身份证号：`1111111`
3. 点击"验证身份"
4. 点击摄像头区域
5. 应该能正常启动摄像头

---

## 🎯 成功标志

摄像头启动成功后，您应该看到：

1. ✅ 绿色提示："摄像头已启动"
2. ✅ 实时视频画面
3. ✅ "拍照并识别"按钮
4. ✅ "关闭摄像头"按钮

---

## 💡 常见问题

### Q1: 为什么会出现 NotReadableError？

**A:** 最常见的原因是摄像头被其他程序占用，例如：
- 微信视频通话
- QQ视频
- 钉钉会议
- Zoom会议
- 其他浏览器标签页

### Q2: 如何确认摄像头是否被占用？

**A:** 
1. 打开任务管理器（Ctrl + Shift + Esc）
2. 查看"进程"标签
3. 查找视频会议软件或通讯软件
4. 如果这些程序正在运行，可能占用了摄像头

### Q3: 关闭程序后还是不行？

**A:** 
1. 完全关闭浏览器（所有窗口）
2. 重新打开浏览器
3. 访问 http://localhost:5173
4. 重新测试

### Q4: 可以使用手机拍照上传吗？

**A:** 当前版本需要使用摄像头实时拍照。如果摄像头无法使用，可以考虑：
1. 使用其他电脑
2. 使用外接USB摄像头
3. 修复当前摄像头问题

---

## 🔍 调试信息

### 检查摄像头是否可用

在浏览器控制台（F12 → Console）运行：

```javascript
navigator.mediaDevices.getUserMedia({ video: true })
  .then(stream => {
    console.log('✅ 摄像头可用')
    stream.getTracks().forEach(track => track.stop())
  })
  .catch(err => {
    console.error('❌ 摄像头不可用:', err.name, err.message)
  })
```

### 查看可用设备

```javascript
navigator.mediaDevices.enumerateDevices()
  .then(devices => {
    const cameras = devices.filter(d => d.kind === 'videoinput')
    console.log('可用摄像头:', cameras.length)
    cameras.forEach(c => console.log(c.label))
  })
```

---

## 🚨 立即操作

### 现在请按照以下步骤操作：

1. **关闭可能占用摄像头的程序**
   - 微信
   - QQ
   - 钉钉
   - Zoom
   - Teams

2. **刷新浏览器**
   ```
   Ctrl + Shift + R
   ```

3. **重新测试**
   - 进入离场安检
   - 验证身份
   - 点击摄像头区域

4. **如果还是不行**
   - 完全关闭浏览器
   - 重新打开
   - 再次测试

---

## 📞 仍然无法解决？

如果按照上述步骤操作后仍然无法使用摄像头，请提供：

1. **错误信息**
   - 浏览器控制台的完整错误信息

2. **系统信息**
   - 操作系统版本
   - 浏览器名称和版本

3. **摄像头信息**
   - 是否是外接摄像头
   - 摄像头型号

4. **已尝试的方法**
   - 列出您已经尝试过的解决方案

---

**最后更新：** 2026-03-02  
**问题类型：** 摄像头被占用  
**解决方案：** 关闭占用程序并刷新浏览器
