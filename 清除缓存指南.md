# 🔧 清除浏览器缓存指南

## 问题说明

您看到的 404 错误是**浏览器缓存**导致的。后端服务实际上已经正常运行，所有 API 都在正常响应。

## ✅ 系统状态确认

- ✅ 后端服务运行正常（端口 8888）
- ✅ 前端服务运行正常（端口 5173）
- ✅ API 代理配置正确
- ✅ 所有接口响应正常

## 🚀 快速解决方案

### 方案 1：强制刷新（最简单）⭐

在浏览器中按以下快捷键：

**Windows/Linux:**
- `Ctrl + Shift + R`
- 或 `Ctrl + F5`

**Mac:**
- `Cmd + Shift + R`

### 方案 2：开发者工具强制刷新

1. 按 `F12` 打开开发者工具
2. 右键点击浏览器的刷新按钮（地址栏左边）
3. 选择"清空缓存并硬性重新加载"

### 方案 3：清除特定网站缓存

**Chrome/Edge:**
1. 按 `F12` 打开开发者工具
2. 右键点击刷新按钮
3. 选择"清空缓存并硬性重新加载"

**Firefox:**
1. 按 `Ctrl + Shift + Delete`
2. 选择"缓存"
3. 时间范围选择"全部"
4. 点击"立即清除"

### 方案 4：使用无痕模式测试

1. 打开浏览器无痕/隐私模式：
   - Chrome/Edge: `Ctrl + Shift + N`
   - Firefox: `Ctrl + Shift + P`
2. 访问 `http://localhost:5173`
3. 验证功能是否正常

### 方案 5：禁用缓存（开发时推荐）

1. 按 `F12` 打开开发者工具
2. 切换到 Network（网络）标签
3. 勾选"Disable cache"（禁用缓存）
4. 保持开发者工具打开状态
5. 刷新页面

## 🧪 验证修复

### 步骤 1：打开测试页面

在浏览器中打开：
```
file:///C:/Users/xxzx-admin/Desktop/智能安检系统/test-api.html
```

或者直接双击项目根目录下的 `test-api.html` 文件。

### 步骤 2：查看测试结果

测试页面会自动测试所有 API 接口，您应该看到：
- ✅ 根路径测试：200 OK
- ✅ 统计数据接口：200 OK
- ✅ 最近记录接口：200 OK
- ✅ 异常记录接口：200 OK
- ✅ 通道信息接口：200 OK

### 步骤 3：验证主应用

1. 访问 `http://localhost:5173`
2. 按 `F12` 打开开发者工具
3. 切换到 Network（网络）标签
4. 刷新页面
5. 查看 API 请求状态（应该都是 200）

## 📊 预期结果

清除缓存后，您应该看到：

### 数据看板
- 今日入场：156 人
- 今日离场：142 人
- 今日异常：0 次
- 当前在场：14 人

### 最近安检记录
显示最近的入场和离场记录

### 通道使用情况
显示 4 个通道的使用状态

## 🔍 如何确认问题已解决

### 检查 1：Network 标签
1. 按 `F12` 打开开发者工具
2. 切换到 Network 标签
3. 刷新页面
4. 查找以下请求：
   - `/api/statistics` → 应该是 200（不是 404）
   - `/api/recent-records` → 应该是 200
   - `/api/recent-anomalies` → 应该是 200
   - `/api/channels` → 应该是 200

### 检查 2：Console 标签
1. 切换到 Console（控制台）标签
2. 不应该看到 404 错误
3. 可能会看到一些正常的日志信息

### 检查 3：页面显示
- 数据看板显示真实数字（不是 0）
- 最近记录表格有数据
- 通道卡片显示使用情况

## 🎯 常见问题

### Q1: 强制刷新后还是 404？

**A:** 尝试以下步骤：
1. 完全关闭浏览器（所有窗口）
2. 重新打开浏览器
3. 访问 `http://localhost:5173`

### Q2: 无痕模式可以，正常模式不行？

**A:** 说明确实是缓存问题，需要清除浏览器缓存：
1. 浏览器设置 → 隐私和安全
2. 清除浏览器数据
3. 选择"缓存的图片和文件"
4. 时间范围选择"全部时间"
5. 清除数据

### Q3: 所有方法都试过了还是不行？

**A:** 重启开发服务器：

**停止前端服务：**
1. 找到运行 `npm run dev` 的终端
2. 按 `Ctrl + C` 停止服务

**重新启动前端：**
```bash
npm run dev
```

**停止后端服务：**
1. 找到运行 `python main.py` 的终端
2. 按 `Ctrl + C` 停止服务

**重新启动后端：**
```bash
cd backend
python main.py
```

## 💡 开发建议

为了避免将来遇到类似问题，建议：

1. **开发时禁用缓存**
   - 打开开发者工具（F12）
   - Network 标签
   - 勾选"Disable cache"

2. **使用无痕模式开发**
   - 避免缓存干扰
   - 每次都是全新环境

3. **定期清理缓存**
   - 特别是修改 API 后
   - 或者看到奇怪的错误时

## 📞 仍然无法解决？

如果上述所有方法都无效，请提供以下信息：

1. **浏览器信息**
   - 浏览器名称和版本
   - 操作系统

2. **Network 标签截图**
   - 显示 API 请求的状态码
   - 显示请求的详细信息

3. **Console 标签内容**
   - 复制所有错误信息

4. **测试页面结果**
   - 打开 `test-api.html`
   - 截图测试结果

---

**最后更新：** 2026-03-02  
**系统版本：** v1.0
