# 数据看板与异常记录 - 真实数据更新总结

## ✅ 已完成的修改

### 后端修改（backend/main.py）

#### 1. 数据存储结构更新
```python
# 新增数据存储
check_in_records = []   # 入场记录列表
check_out_records = []  # 离场记录列表
anomalies_db = []       # 异常记录列表
security_records = {}   # 当前在场记录
```

#### 2. 新增 API 接口（4 个）

**统计数据接口**
```python
GET /api/statistics
- 今日入场数（基于日期统计）
- 今日离场数（基于日期统计）
- 今日异常数（基于日期统计）
- 当前在场人数（security_records 长度）
```

**最近记录接口**
```python
GET /api/recent-records?limit=10
- 合并入场和离场记录
- 按时间倒序排列
- 返回最近 N 条记录
```

**最近异常接口**
```python
GET /api/recent-anomalies?limit=10
- 返回最近 N 条异常记录
- 按时间倒序排列
```

**通道信息接口**
```python
GET /api/channels
- 4 个通道的实时状态
- 今日通过人数统计
- 当前使用用户
```

#### 3. 更新入场接口
```python
POST /api/checkin
- 保存到 security_records（当前在场）
- 保存到 check_in_records（历史记录）
- 添加格式化时间字段
```

#### 4. 更新离场接口
```python
POST /api/checkout
- 调用 AI 识别
- 自动比对物品
- 检测异常（多带、少带、数量、重量）
- 保存离场记录
- 保存异常记录
- 正常离场删除 security_records
```

#### 5. 新增比对函数
```python
def compare_items(entry_items, exit_items)
- 检测多带物品
- 检测物品缺失
- 检测数量异常
- 返回异常列表
```

### 前端修改

#### 1. Dashboard.vue（数据看板）

**数据加载函数**
```javascript
loadStatistics()      // 加载统计数据
loadRecentRecords()   // 加载最近记录
loadRecentAnomalies() // 加载最近异常
loadChannels()        // 加载通道信息
```

**自动刷新**
```javascript
// 每 10 秒自动刷新所有数据
setInterval(refreshAll, 10000)
```

**生命周期**
```javascript
onMounted()   // 初始加载数据
onUnmounted() // 清除定时器
```

#### 2. Anomalies.vue（异常记录）

**数据加载**
```javascript
loadAnomalies() // 加载异常记录列表
- 支持分页
- 支持筛选
- 支持查询
```

**异常处理**
```javascript
handleAnomaly() // 处理异常
- 输入处理备注
- 更新状态为"已处理"
- 刷新列表
```

#### 3. API 接口（src/api/index.js）

**新增接口**
```javascript
getStatistics()        // 获取统计数据
getRecentRecords()     // 获取最近记录
getRecentAnomalies()   // 获取最近异常
getChannels()          // 获取通道信息
```

## 🔄 数据流程图

### 完整流程

```
用户入场
    ↓
身份验证 → 生成 userId
    ↓
摄像头拍照 → AI 识别
    ↓
保存入场记录
    ├─ security_records[userId] = {...}  (当前在场)
    └─ check_in_records.append({...})   (历史记录)
    ↓
更新统计数据
    ├─ 今日入场 +1
    └─ 当前在场 +1
    ↓
数据看板自动刷新（10秒后）
    ↓
用户离场
    ↓
身份验证 → 匹配 userId
    ↓
摄像头拍照 → AI 识别
    ↓
自动比对物品
    ├─ 比对物品名称
    ├─ 比对物品数量
    └─ 比对物品重量
    ↓
判断结果
    ├─ 正常：
    │   ├─ 删除 security_records[userId]
    │   ├─ check_out_records.append({...})
    │   ├─ 今日离场 +1
    │   └─ 当前在场 -1
    │
    └─ 异常：
        ├─ 保留 security_records[userId]
        ├─ check_out_records.append({...})
        ├─ anomalies_db.append({...})
        ├─ 今日离场 +1
        └─ 今日异常 +1
    ↓
数据看板自动刷新
```

## 📊 数据结构

### 入场记录
```javascript
{
  userId: "USER_1234567890",
  type: "entry",
  items: [
    { name: "笔记本电脑", category: "电子产品", quantity: 1, weight: 1.5 },
    { name: "手机", category: "电子产品", quantity: 1, weight: 0.2 }
  ],
  totalCount: 2,
  totalWeight: 1.7,
  checkTime: "2024-03-02T14:30:00",
  checkTimeFormatted: "2024-03-02 14:30:00",
  channelNo: 1
}
```

### 异常记录
```javascript
{
  id: "AN001",
  userId: "USER_1234567890",
  type: "extra",                    // extra/missing/quantity/weight
  typeText: "多带物品",
  time: "2024-03-02 14:15:22",
  status: "pending",                // pending/resolved
  entryTime: "2024-03-02 09:30:15",
  exitTime: "2024-03-02 14:15:22",
  entryChannel: 1,
  exitChannel: 2,
  entryItems: [...],
  exitItems: [...],
  anomalyDescription: "离场时发现\"平板电脑\"，入场记录中不存在该物品"
}
```

## 🎯 功能特点

### 1. 实时统计
- ✅ 基于真实数据计算
- ✅ 按日期统计今日数据
- ✅ 实时更新当前在场人数

### 2. 自动刷新
- ✅ 每 10 秒自动刷新
- ✅ 无需手动刷新
- ✅ 数据实时同步

### 3. 异常检测
- ✅ 自动比对入场/离场物品
- ✅ 检测多带物品
- ✅ 检测物品缺失
- ✅ 检测数量异常

### 4. 数据追溯
- ✅ 完整的入场记录
- ✅ 完整的离场记录
- ✅ 完整的异常记录
- ✅ 支持历史查询

## 🧪 测试场景

### 场景 1：正常入场离场

**操作步骤：**
1. 用户 A 入场，识别到：笔记本、手机
2. 查看数据看板：今日入场 = 1，当前在场 = 1
3. 用户 A 离场，识别到：笔记本、手机
4. 查看数据看板：今日离场 = 1，当前在场 = 0

**预期结果：**
- ✅ 统计数据正确
- ✅ 无异常记录
- ✅ 用户 A 从在场列表移除

### 场景 2：多带物品异常

**操作步骤：**
1. 用户 B 入场，识别到：笔记本、手机
2. 用户 B 离场，识别到：笔记本、手机、平板
3. 查看数据看板：今日异常 = 1
4. 查看异常记录：显示"多带物品"异常

**预期结果：**
- ✅ 检测到多带"平板"
- ✅ 生成异常记录
- ✅ 用户 B 仍在在场列表

### 场景 3：物品缺失异常

**操作步骤：**
1. 用户 C 入场，识别到：笔记本、手机、钱包
2. 用户 C 离场，识别到：笔记本、手机
3. 查看异常记录：显示"物品缺失"异常

**预期结果：**
- ✅ 检测到缺失"钱包"
- ✅ 生成异常记录
- ✅ 异常描述准确

### 场景 4：数据看板自动刷新

**操作步骤：**
1. 打开数据看板
2. 在另一个标签页进行入场操作
3. 等待 10 秒
4. 观察数据看板

**预期结果：**
- ✅ 数据自动更新
- ✅ 无需手动刷新
- ✅ 最近记录显示新数据

## 📝 注意事项

### 1. 数据持久化

⚠️ **当前数据存储在内存中**
- 重启后端服务会清空所有数据
- 适合开发和测试环境
- 生产环境需要使用数据库

### 2. 时间统计

✅ **今日统计基于日期**
```python
today = datetime.now().date()
today_check_in = sum(1 for record in check_in_records 
                     if datetime.fromisoformat(record["checkTime"]).date() == today)
```

### 3. 异常检测逻辑

✅ **比对规则**
- 物品名称完全匹配
- 数量必须一致
- 重量允许误差（未实现）

### 4. 自动刷新

✅ **刷新机制**
- 使用 setInterval 定时刷新
- 组件卸载时清除定时器
- 避免内存泄漏

## 🚀 快速测试

### 1. 启动系统

```bash
# 终端 1：启动后端
cd backend
python main.py

# 终端 2：启动前端
npm run dev
```

### 2. 访问页面

- 数据看板：http://localhost:5173/dashboard
- 异常记录：http://localhost:5173/anomalies

### 3. 测试流程

1. 进行 2-3 次入场安检
2. 查看数据看板统计数据
3. 进行 1-2 次正常离场
4. 进行 1 次异常离场（故意拍摄不同物品）
5. 查看异常记录页面
6. 处理异常记录

## ✅ 验收清单

### 后端验收
- [x] 统计数据接口返回正确
- [x] 最近记录接口返回正确
- [x] 最近异常接口返回正确
- [x] 通道信息接口返回正确
- [x] 异常检测逻辑正确

### 前端验收
- [x] 数据看板显示真实数据
- [x] 自动刷新功能正常
- [x] 异常记录列表显示正确
- [x] 异常详情展示完整
- [x] 异常处理功能正常

### 功能验收
- [ ] 入场数据正确保存
- [ ] 离场数据正确保存
- [ ] 异常检测准确
- [ ] 统计计算正确
- [ ] 自动刷新正常

## 📚 相关文档

- `真实数据功能说明.md` - 详细功能说明
- `摄像头功能测试指南.md` - 摄像头测试流程
- `快速启动指南.md` - 系统启动指南

---

**更新时间：** 2024-03-02  
**状态：** 代码已完成，等待测试  
**下一步：** 启动系统进行功能测试
