# 故障排查指南 - 404 错误解决

## 🐛 问题描述

浏览器控制台显示多个 404 错误：
- `api/recent-anomalies?limit=4` - 404
- `dashboard.vue?t=1` - 404
- `api/channels` - 404
- 等等...

## 🔍 问题原因

可能的原因：
1. ❌ 后端服务未启动
2. ❌ 后端服务启动失败（模块导入错误）
3. ❌ 端口不匹配
4. ❌ API 路由未正确定义

## 🛠️ 解决方案

### 方案 1：检查后端服务状态（最常见）

#### 步骤 1：检查后端是否运行

**Windows:**
```cmd
netstat -ano | findstr :8888
```

**如果没有输出，说明后端未运行！**

#### 步骤 2：启动后端服务

```bash
cd backend
python main.py
```

**观察输出：**
- ✅ 如果看到 "Uvicorn running on http://0.0.0.0:8888"，说明启动成功
- ❌ 如果看到错误信息，继续下面的步骤

---

### 方案 2：测试后端启动（如果方案 1 失败）

#### 步骤 1：运行测试脚本

```bash
cd backend
python test_startup.py
```

**查看输出，找出失败的模块：**

```
✅ FastAPI 导入成功
✅ python-dotenv 导入成功
❌ OpenAI SDK 导入失败: No module named 'openai'
```

#### 步骤 2：安装缺失的依赖

```bash
cd backend
pip install -r requirements.txt
```

**或者单独安装：**
```bash
pip install fastapi uvicorn python-dotenv openai pillow
```

#### 步骤 3：重新启动后端

```bash
python main.py
```

---

### 方案 3：使用简化版后端（临时方案）

如果完整版后端无法启动，可以先使用简化版：

```bash
cd backend
python main_simple.py
```

**简化版特点：**
- ✅ 不依赖 AI 服务
- ✅ 只提供数据看板和异常记录接口
- ✅ 可以测试前端连接

---

### 方案 4：检查端口冲突

#### 步骤 1：检查 8888 端口是否被占用

**Windows:**
```cmd
netstat -ano | findstr :8888
```

**如果被占用，找到进程 ID (PID)，然后结束进程：**
```cmd
taskkill /PID <进程ID> /F
```

#### 步骤 2：或者修改端口

编辑 `backend/main.py` 最后一行：
```python
uvicorn.run(app, host="0.0.0.0", port=8889)  # 改为 8889
```

同时修改 `vite.config.js`：
```javascript
proxy: {
  '/api': {
    target: 'http://localhost:8889',  // 改为 8889
    changeOrigin: true
  }
}
```

---

### 方案 5：检查 .env 文件

#### 步骤 1：确认 .env 文件存在

```bash
cd backend
dir .env  # Windows
ls -la .env  # Linux/Mac
```

#### 步骤 2：检查内容

```bash
type .env  # Windows
cat .env   # Linux/Mac
```

**应该包含：**
```
DASHSCOPE_API_KEY=sk-88ed49180e7d4462b16b8f2902f63c7a
QWEN_MODEL=qwen3-vl-plus
QWEN_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
```

#### 步骤 3：如果不存在，创建它

```bash
copy .env.example .env  # Windows
cp .env.example .env    # Linux/Mac
```

然后编辑 `.env` 文件，填入 API Key。

---

## 🧪 验证修复

### 1. 测试后端 API

**方法 1：浏览器访问**
```
http://localhost:8888/
```

**应该看到：**
```json
{"message": "智能安检系统 API", "status": "running"}
```

**方法 2：测试统计接口**
```
http://localhost:8888/api/statistics
```

**应该看到：**
```json
{
  "todayCheckIn": 0,
  "todayCheckOut": 0,
  "todayAnomalies": 0,
  "currentInside": 0
}
```

### 2. 测试前端连接

1. 刷新前端页面（F5）
2. 打开浏览器控制台（F12）
3. 查看 Network 标签
4. 应该看到 API 请求返回 200（而不是 404）

---

## 📝 常见错误及解决

### 错误 1：ModuleNotFoundError: No module named 'openai'

**解决：**
```bash
pip install openai
```

### 错误 2：ModuleNotFoundError: No module named 'dotenv'

**解决：**
```bash
pip install python-dotenv
```

### 错误 3：ModuleNotFoundError: No module named 'app.services'

**解决：**
检查目录结构，确保存在：
```
backend/
  app/
    services/
      qwen_service.py
    config/
      settings.py
    prompts/
      security_check.py
```

### 错误 4：ValueError: DASHSCOPE_API_KEY 未配置

**解决：**
1. 检查 `.env` 文件是否存在
2. 检查 API Key 是否正确
3. 重启后端服务

### 错误 5：Address already in use

**解决：**
端口 8888 被占用，结束占用进程或更改端口。

---

## 🚀 快速修复命令

### Windows 快速修复

```cmd
# 1. 进入后端目录
cd backend

# 2. 安装依赖
pip install -r requirements.txt

# 3. 检查 .env 文件
if not exist .env copy .env.example .env

# 4. 启动服务
python main.py
```

### Linux/Mac 快速修复

```bash
# 1. 进入后端目录
cd backend

# 2. 安装依赖
pip install -r requirements.txt

# 3. 检查 .env 文件
[ ! -f .env ] && cp .env.example .env

# 4. 启动服务
python main.py
```

---

## ✅ 成功标志

当你看到以下输出时，说明后端启动成功：

```
==================================================
系统配置：
  API Key: sk-88ed49...
  模型: qwen3-vl-plus
  缓存: 启用
==================================================
✅ AI 识别服务初始化成功
==================================================
智能安检系统后端服务启动
API 文档: http://localhost:8888/docs
==================================================
INFO:     Started server process [12345]
INFO:     Waiting for application startup.
INFO:     Application startup complete.
INFO:     Uvicorn running on http://0.0.0.0:8888 (Press CTRL+C to quit)
```

---

## 📞 仍然无法解决？

### 收集诊断信息

运行以下命令并提供输出：

```bash
# 1. Python 版本
python --version

# 2. 已安装的包
pip list

# 3. 测试启动
cd backend
python test_startup.py

# 4. 端口检查
netstat -ano | findstr :8888  # Windows
lsof -i :8888                 # Linux/Mac
```

### 查看日志

1. 后端终端的完整输出
2. 浏览器控制台的错误信息
3. Network 标签的请求详情

---

**最后更新：** 2024-03-02  
**适用版本：** v1.0
