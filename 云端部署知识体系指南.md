# 🌐 云端项目部署理论知识全景指南

本指南旨在帮助你系统性地掌握将“智能安检系统”这类 **FastAPI (Python) + Vue/Vite (Node.js)** 全栈项目从本地开发环境迁移到云端（腾讯云/阿里云/AWS）所涉及的核心知识。

---

## 一、 网络架构与安全基础 (Networking & Security)

作为部署的第一步，你必须理解“请求是如何进入服务器的”。

### 1.1 端口 (Port) 与 协议 (Protocol)
*   **概念**：服务器就像一栋有 65535 个房间的大楼，每一个服务（如 Web、SSH、数据库）都住在一个房间里。
*   **关键端口**：
    *   `22` (SSH)：用于远程管理。
    *   `80`/`443`：标准 Web 流量（HTTP/HTTPS）。
    *   `5173`：Vite 开发服务器的默认房间。
    *   `8888`：FastAPI 后端服务的默认房间。
*   **TCP 协议**：我们部署的服务大多基于 TCP（传输控制协议），它确保了数据的完整性和可靠传输。

### 1.2 云服务器防火墙/安全组 (Security Group)
*   **规则理论**：默认情况下，云服务商（腾讯云/阿里云）会关闭所有入站流量。你必须手动创建“允许”规则（Inbound Rules），指定特定端口和 IP 范围。
*   **操作要点**：如果不开放端口，即使服务器内部服务运行正常，外网用户也无法通过浏览器访问。

---

## 二、 Linux 系统运维核心 (Linux Administration)

绝大多数云服务器运行的是 Ubuntu 或 CentOS，掌握命令行是基本功。

### 2.1 目录结构与路径
*   `~` 或 `/root`：当前用户的家目录。
*   `/etc`：系统配置文件存放地。
*   `/var/log`：排错时查阅日志的地方。

### 2.2 核心管理指令
*   **文件操作**：`ls -lh` (列表查看), `unzip` (解压缩), `cd` (切换目录)。
*   **权限管理 (`chmod`)**：在 Linux 中，执行脚本（如 Vite 或 Start.sh）需要赋予它“执行权限” (Executable bit)，常用的命令是 `chmod +x`。
*   **进程管理**：
    *   `ps aux`：列出当前系统中运行的所有“活”进程。
    *   `kill -9 [PID]`：强制终止失控或需要重启的程序。
    *   `ss -tuln`：查看当前哪些端口正在被占用。

### 2.3 后台运行与守护 (`nohup`)
*   **理论**：普通运行命令在 SSH 终端关闭后会收到挂断信号（SIGHUP），导致程序停止。
*   **解决方案**：使用 `nohup [command] &`。其中 `nohup` 忽略挂断信号，`&` 将任务转入后台。

---

## 三、 Python 后端运行环境 (Backend Runtime)

### 3.1 虚拟环境 (Virtual Environment - `venv`)
*   **为什么需要**？Linux 系统自带 Python 关键组件，直接用 `pip install` 可能会导致系统依赖冲突。
*   **隔离理论**：`venv` 会在项目目录下创建一个“沙盒”，所有的库（如 FastAPI, OpenAI）都安装在沙盒里，不影响外界。

### 3.2 ASGI 服务器 (Uvicorn)
*   **异步模型**：FastAPI 是高性能异步框架，需要 Uvicorn 这种兼容 ASGI 协议的服务器来承载。
*   **监听地址**：必须理解 `host=0.0.0.0` 的意义——它告诉程序“监听这台服务器上的所有网口”，而不仅仅是内部回环地址 (`127.0.0.1`)。

---

## 四、 前端构建与 Node.js (Frontend & Node)

### 4.1 包管理器 (NPM/Yarn)
*   **依赖树**：掌握 `package.json`（清单）和 `node_modules`（仓库）的关系。

### 4.2 Vite 开发服务器与构建
*   **开发模式 (Dev Mode)**：利用 Vite 极快的 HMR（热更新）特性，在云端实时运行项目。
*   **网络代理 (Proxying)**：理解前端在 `5173` 是如何通过配置（`vite.config.js`）将接口请求代理转给 `8888` 后端的，这是解决跨域问题（CORS）的常用手段。

---

## 五、 DevOps 思维：部署全链路

最后，你需要构建一个全局视角。一个完整的部署链路如下：

1.  **打包 (Packaging)**：本地代码清理与压缩。
2.  **传输 (Transferring)**：通过 SFTP 或 Git 同步到云端。
3.  **基建 (Provisioning)**：安装 Runtime（Python, Node.js）。
4.  **初始化 (Dependency)**：安装项目特有的所有依赖包。
5.  **启动与监控 (Running & Logging)**：后台运行并重定向输出到日志文件。
6.  **验证 (Verification)**：外网连通性测试。

---

> [!IMPORTANT]
> **部署成功的核心心法：**
> “看日志，查端口，对配置”。
> 大多数部署失败的原因只有三个：**防火墙没开端口**、**服务没启动**、或是**监听地址设置为了本地私有 (127.0.0.1) 而非全网口 (0.0.0.0)**。
