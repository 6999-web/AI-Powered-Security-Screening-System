# 智能安检系统 - 使用快速参考

**系统版本**: X 光安检机模拟 Demo v1.0  
**最后更新**: 2026-03-11  
**状态**: ✅ 已验证并可投入使用

---

## 🚀 快速启动

### 1. 启动后端服务

```bash
# 方式 1: 直接运行
python backend/main_xray_demo.py

# 方式 2: 使用启动脚本 (Windows)
start_xray_demo.bat

# 方式 3: 使用启动脚本 (Linux/Mac)
./start_xray_demo.sh
```

**验证后端已启动**:
- 访问 http://localhost:8888
- 应该看到 `{"status": "running"}`

### 2. 启动前端服务

```bash
# 首次运行需要安装依赖
npm install

# 启动开发服务器
npm run dev
```

**验证前端已启动**:
- 访问 http://localhost:5173
- 应该看到安检系统界面

### 3. 运行功能测试

```bash
python 功能测试脚本.py
```

**预期结果**: 所有 9 个测试通过，成功率 100%

---

## 📱 系统使用流程

### 场景 1: 用户入场

**步骤**:
1. 打开前端应用 (http://localhost:5173)
2. 点击 "入场安检" 菜单
3. 输入身份证号 (例如: 123456789012345678)
4. 点击 "拍照" 或 "上传图片"
5. 选择一张图片
6. 点击 "识别" 按钮
7. 等待 AI 识别结果
8. 查看识别的物品列表
9. 点击 "确认入场" 完成

**预期结果**:
- 系统识别出物品 (例如: 手机、钱包等)
- 记录入场信息
- 显示成功提示

### 场景 2: 用户离场

**步骤**:
1. 点击 "离场安检" 菜单
2. 输入身份证号 (必须与入场时相同)
3. 点击 "拍照" 或 "上传图片"
4. 选择一张图片
5. 点击 "识别" 按钮
6. 等待 AI 识别结果
7. 系统自动与入场物品比对
8. 查看比对结果和异常检测
9. 点击 "确认离场" 完成

**预期结果**:
- 系统识别出物品
- 与入场物品比对
- 如果物品相同，显示 "正常"
- 如果物品不同，显示 "异常" 并列出差异

### 场景 3: 查看数据看板

**步骤**:
1. 点击 "数据看板" 菜单
2. 查看统计数据:
   - 总入场人数
   - 总离场人数
   - 总异常数
   - 当前在场人数
3. 查看最近记录
4. 查看通道信息

**预期结果**:
- 显示实时统计数据
- 显示最近的入场离场记录
- 显示各通道状态

### 场景 4: 查看异常记录

**步骤**:
1. 点击 "异常记录" 菜单
2. 查看所有异常记录
3. 点击异常记录查看详细信息

**预期结果**:
- 显示所有异常记录
- 显示异常类型和详细信息
- 显示异常发生时间

---

## 🔍 常见问题

### Q1: 系统无法连接到后端

**症状**: 前端显示 "无法连接到服务器"

**解决方案**:
1. 确保后端服务已启动: `python backend/main_xray_demo.py`
2. 检查后端是否运行在 http://localhost:8888
3. 检查防火墙设置
4. 重启后端服务

### Q2: 图片识别失败

**症状**: 点击 "识别" 后显示错误

**解决方案**:
1. 确保 API Key 已正确配置在 `backend/.env`
2. 检查网络连接
3. 尝试使用不同的图片
4. 查看后端日志获取详细错误信息

### Q3: 身份验证失败

**症状**: 输入身份证号后无法验证

**解决方案**:
1. 确保身份证号格式正确 (18 位数字)
2. 检查后端数据库是否正常
3. 重启后端服务

### Q4: 物品比对不准确

**症状**: 离场时物品比对结果不正确

**解决方案**:
1. 确保入场和离场使用相同的身份证号
2. 确保上传的图片清晰
3. 检查 AI 识别结果是否准确
4. 查看系统日志获取详细信息

---

## 📊 API 端点参考

### 身份验证

```bash
POST /api/verify-identity
Content-Type: application/x-www-form-urlencoded

idCard=123456789012345678

# 响应
{
  "userId": "USER_345678",
  "idCard": "123456789012345678"
}
```

### 入场安检

```bash
POST /api/entry
Content-Type: multipart/form-data

image: <图片文件>
user_id: USER_345678
channel_no: 1

# 响应
{
  "record_id": "ENTRY_20260311180532_c163fc41",
  "items": [
    {"name": "手机", "quantity": 1},
    {"name": "钱包", "quantity": 1}
  ],
  "total_count": 2
}
```

### 离场安检

```bash
POST /api/exit
Content-Type: multipart/form-data

image: <图片文件>
user_id: USER_345678
channel_no: 1

# 响应
{
  "record_id": "EXIT_20260311180536_f46fc96b",
  "items": [
    {"name": "手机", "quantity": 1},
    {"name": "钱包", "quantity": 1}
  ],
  "comparison_status": "normal",
  "anomalies": []
}
```

### 获取统计数据

```bash
GET /api/statistics

# 响应
{
  "total_entry": 1,
  "total_exit": 1,
  "total_alerts": 0,
  "current_inside": 0
}
```

### 获取最近记录

```bash
GET /api/recent-records?limit=5

# 响应
{
  "records": [
    {
      "type": "exit",
      "user_id": "USER_345678",
      "total_count": 2,
      "timestamp": "2026-03-11T18:05:36"
    }
  ]
}
```

### 获取异常记录

```bash
GET /api/anomalies?limit=10

# 响应
{
  "anomalies": []
}
```

### 获取通道信息

```bash
GET /api/channels

# 响应
{
  "channels": [
    {"id": 1, "name": "通道 1", "status": "active"},
    {"id": 2, "name": "通道 2", "status": "active"},
    {"id": 3, "name": "通道 3", "status": "idle"}
  ]
}
```

### 获取比对结果

```bash
GET /api/compare/USER_345678

# 响应
{
  "user_id": "USER_345678",
  "entry_items": [
    {"name": "手机", "quantity": 1},
    {"name": "钱包", "quantity": 1}
  ],
  "exit_items": [
    {"name": "手机", "quantity": 1},
    {"name": "钱包", "quantity": 1}
  ],
  "status": "normal"
}
```

---

## 🛠️ 系统配置

### 环境变量 (`backend/.env`)

```
# 通义千问 API 配置
QWEN_API_KEY=sk-eadde50eff9b4a149bb3fe222dde932a
QWEN_MODEL=qwen-vl-plus

# 数据库配置
DATABASE_URL=sqlite:///security_system.db

# 服务器配置
HOST=0.0.0.0
PORT=8888
```

### 依赖安装

**后端依赖**:
```bash
pip install -r backend/requirements_xray_demo.txt
```

**前端依赖**:
```bash
npm install
```

---

## 📈 性能优化建议

1. **图片优化**: 使用压缩的图片以加快识别速度
2. **缓存**: 启用浏览器缓存以加快前端加载
3. **数据库**: 定期清理旧数据以保持数据库性能
4. **API 限流**: 考虑添加 API 限流以防止滥用

---

## 🔐 安全建议

1. **API Key 保护**: 不要在代码中硬编码 API Key，使用环境变量
2. **身份验证**: 考虑添加用户认证机制
3. **数据加密**: 对敏感数据进行加密存储
4. **访问控制**: 限制 API 访问权限
5. **日志记录**: 记录所有重要操作用于审计

---

## 📚 相关文档

- `README.md` - 项目概述
- `DEVELOPMENT_PLAN.md` - 开发方案
- `完整功能测试报告.md` - 详细测试报告
- `系统验证完成总结.md` - 系统验证总结
- `快速启动指南.md` - 快速启动指南

---

## 💬 获取帮助

如有任何问题，请：

1. 查看相关文档
2. 检查系统日志
3. 运行功能测试脚本
4. 联系技术支持

---

**最后更新**: 2026-03-11  
**版本**: 1.0.0  
**状态**: ✅ 已验证
